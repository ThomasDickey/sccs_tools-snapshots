# $Id: makefile.in,v 6.3 2010/06/29 10:55:49 tom Exp $
# Top-level makefile for SCCS_TOOLS put-delta utility

THIS	= putdelta

@make_include_left@@TD_LIB_rules@/td_lib.mk@make_include_right@

####### (Standard Lists) #######################################################
B	= ../../bin

IT	=\
	$B/$(THIS)\
	$B/sccsput

DIRS	=\
	bin

####### (Standard Productions) #################################################
all\
run_test::	$(DIRS)

all\
clean\
distclean\
run_test\
lint.out::
	cd src;		$(MAKE) $@
	cd test;	$(MAKE) $@

clean\
distclean::		; $(RM) $(CLEAN)

distclean::		; $(RM) -r $(DIRS)
distclean::		; $(RM) */makefile

destroy::		; $(DESTROY)

install:	all $(IT)
uninstall:		; $(RM) $(IT)

####### (Details of Productions) ###############################################
$(DIRS):			; mkdir $@
$B/$(THIS):	bin/$(THIS)	; $(INSTALL_PROGRAM) $? $@
$B/sccsput:	bin/sccsput	; $(INSTALL_PROGRAM) $? $@
